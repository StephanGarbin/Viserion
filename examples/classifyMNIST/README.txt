To run the example, navigate the directory containing the model.lua, io.lua etc. files.

Then run:

th Viserion/Viserion.lua -ioFile io.lua -modelFile model.lua -criterionFile criterion.lua -optimFile optim.lua -doTraining -batchSize 500 -printCLErrors

Happy Coding!
